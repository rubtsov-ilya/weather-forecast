import { FC } from "react";
import FirstSection from "./first-section/FirstSection.tsx";
const HomePage: FC = () => {
  return (
    <main>
      <FirstSection />
    </main>
  );
};

export default HomePage;
